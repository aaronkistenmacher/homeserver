{
  "name": "simpleserver",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "start": "nodemon -w dist dist/server.js",
    "compile": "babel src --presets env --out-dir dist --source-maps",
    "watch": "babel src --presets env --out-dir dist --source-maps --watch",
    "dev-all": "npm run watch & npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aaron Kistenmacher",
  "license": "ISC",
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "express": "^4.16.4",
    "source-map-support": "^0.5.9"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "body-parser": "^1.18.3",
    "eslint": "^5.7.0",
    "eslint-config-airbnb-standard": "^2.1.6",
    "nodemon": "^1.18.4"
  }
}
